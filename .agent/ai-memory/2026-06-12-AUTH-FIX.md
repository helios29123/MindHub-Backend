# 2026-06-12-AUTH-FIX: Cập nhật Database, Sửa lỗi Exception Handler và Auth Logic

## 1. Tóm tắt nhiệm vụ
- Cập nhật database mới nhất theo schema và dữ liệu seed mẫu của dự án.
- Khắc phục lỗi render HTTP status code của `BusinessException` bị đè về `400` sau khi pull code mới.
- Giải quyết các lỗi bảo mật và logic trong xác thực (Auth) được chỉ ra trong review (`feedback.md`).

## 2. Quyết định kỹ thuật & Thiết kế
- **Cập nhật Database**:
  - Tải lại schema [elearning_erd_gd1 (1).sql](file:///d:/laragon/www/MindHub/elearning_erd_gd1%20%281%29.sql) và seed mẫu [elearning_gd1_seed_api_test (1).sql](file:///d:/laragon/www/MindHub/elearning_gd1_seed_api_test%20%281%29.sql).
  - Cập nhật mật khẩu mã hóa bcrypt cho toàn bộ các tài khoản test trong bảng `users` (`12345678`).
- **Sửa Exception Rendering**:
  - Gặp lỗi 15 test cases bị fail sau khi pull code do `bootstrap/app.php` render `BusinessException` dùng `$exception->getCode() ?: 400`. Vì class `BusinessException` không truyền mã lỗi cho parent class Exception, `getCode()` trả về `0` và biến tất cả thành `400`.
  - Khắc phục bằng cách truyền `statusCode` vào `parent::__construct($message, $statusCode)` trong [BusinessException.php](file:///d:/laragon/www/MindHub/MindHub-Backend/BE/app/Exceptions/BusinessException.php) và sửa `bootstrap/app.php` gọi `$exception->getStatusCode()`.
- **Tách biệt auth_sessions và Laravel sessions**:
  - Tạo migration tạo bảng `auth_sessions` phục vụ lưu phiên đăng nhập API của người dùng (chứa `refresh_token_hash`, `device_name` v.v.).
  - Phục hồi lại bảng `sessions` mặc định của Laravel với đầy đủ các cột cho Laravel Session Driver (`id` string, `payload`, `last_activity`).
  - Sửa lại lớp model [AuthSession.php](file:///d:/laragon/www/MindHub/MindHub-Backend/BE/app/Models/AuthSession.php) ánh xạ vào bảng `auth_sessions` thay vì bảng `sessions`.
- **Logic Đăng xuất (Logout)**:
  - Cập nhật hàm `logout` trong [AuthController.php](file:///d:/laragon/www/MindHub/MindHub-Backend/BE/app/Http/Controllers/Api/AuthController.php) để thu hồi đúng phiên đăng nhập dạng session lưu trong DB (lấy qua attribute `auth_session`) thay vì xóa token Sanctum.
- **Xác thực Google OAuth2**:
  - Sử dụng thư viện `google/apiclient` để verify Google ID Token một cách thực chất qua API của Google.
  - Cho phép bỏ qua và dùng mock token dạng base64 decode (bắt đầu bằng `mock_`) trong môi trường `local` và `testing` để hỗ trợ chạy test bộ API nhanh chóng.
- **Rate Limiting & Token Safety**:
  - Áp dụng middleware `throttle:60,1` trên các route đăng nhập, đăng ký, quên mật khẩu, đặt lại mật khẩu trong [auth.php](file:///d:/laragon/www/MindHub/MindHub-Backend/BE/routes/api/auth.php).
  - Trong `forgotPassword()` của `AuthService.php`, không trả `reset_token` trực tiếp trong phản hồi HTTP của API nữa nhằm đảm bảo bảo mật. Thay vào đó, ghi token vào `laravel.log` để phục vụ phát triển/test local.

## 3. Các file đã chỉnh sửa & tạo mới
- **Migration & Model**:
  - [2026_06_12_142114_create_auth_sessions_table.php](file:///d:/laragon/www/MindHub/MindHub-Backend/BE/database/migrations/2026_06_12_142114_create_auth_sessions_table.php) (Tạo mới)
  - [AuthSession.php](file:///d:/laragon/www/MindHub/MindHub-Backend/BE/app/Models/AuthSession.php) (Chỉnh sửa)
- **Controller & Service**:
  - [AuthController.php](file:///d:/laragon/www/MindHub/MindHub-Backend/BE/app/Http/Controllers/Api/AuthController.php) (Chỉnh sửa)
  - [AuthService.php](file:///d:/laragon/www/MindHub/MindHub-Backend/BE/app/Services/AuthService.php) (Chỉnh sửa)
  - [GoogleTokenVerifier.php](file:///d:/laragon/www/MindHub/MindHub-Backend/BE/app/Services/GoogleTokenVerifier.php) (Chỉnh sửa)
- **Core Config & Routing**:
  - [BusinessException.php](file:///d:/laragon/www/MindHub/MindHub-Backend/BE/app/Exceptions/BusinessException.php) (Chỉnh sửa)
  - [app.php](file:///d:/laragon/www/MindHub/MindHub-Backend/BE/bootstrap/app.php) (Chỉnh sửa)
  - [auth.php](file:///d:/laragon/www/MindHub/MindHub-Backend/BE/routes/api/auth.php) (Chỉnh sửa)

## 4. Trạng thái & Việc còn dở
- **Trạng thái**: Hoàn thành 100%. Đã chạy lại bộ test Pest và tất cả 65/65 test cases đều **PASSED**.
- **Việc dở dang**: Không có.
